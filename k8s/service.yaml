apiVersion: v1
kind: Service
metadata:
  name: stock-ticker
  labels:
    app: stock-ticker
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: stock-ticker
