name: Build and Push experiment-ebpf

on:
  push:
    branches: ["main"]
    paths: ["workloads/ebpf-noisy-neighbour/**"]

permissions:
  contents: read
  id-token: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set image tag
        id: tag
        run: |
          TS=$(date -u +%Y%m%d%H%M%S)
          SHA=$(git rev-parse --short HEAD)
          echo "tag=main-${TS}-${SHA}" >> $GITHUB_OUTPUT

      - name: Authenticate to GCP
        id: auth
        uses: google-github-actions/auth@v2
        with:
          token_format: access_token
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_PUSH_SA }}

      - name: Log in to Artifact Registry
        uses: docker/login-action@v3
        with:
          registry: australia-southeast1-docker.pkg.dev
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./workloads/ebpf-noisy-neighbour
          push: true
          platforms: linux/amd64
          tags: australia-southeast1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/experiment-ebpf/experiment-ebpf:${{ steps.tag.outputs.tag }}
